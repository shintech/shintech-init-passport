{
  "name": "shintech-init-passport",
  "version": "0.0.1",
  "description": "Initialize passport local strategy",
  "main": "index.js",
  "repository": "https://github.com/shintech/shintech-init-passport.git",
  "author": "Michael Prather",
  "license": "MIT",
  "scripts": {
    "build": "babel -D -d build/ src/",
    "prepublish": "standard && npm run build"
  },  
  "devDependencies": {
    "babel-cli": "^6.24.0",
    "babel-preset-es2015-node6": "^0.4.0",
    "babel-preset-latest": "^6.24.0",
    "standard": "^10.0.1"
  },
  "engines": {
    "node": ">=6"
}  
}
